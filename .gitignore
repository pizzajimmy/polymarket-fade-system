# ── Database ────────────────────────────────────
# The SQLite DB is runtime.txt data, not source code.
# It lives on the server, not in the repo.
prices.db
*.db
*.db-shm
*.db-wal

# ── Secrets ──────────────────────────────────────
.env
.env.*
!.env.example
secrets.py

# ── Local dashboard artifacts ────────────────────
dashboard/server.txt
dashboard/pmfade.db*

# ── Operator anchor overrides (live judgment calls, not source) ──
anchors_manual.json

# ── Positions data ───────────────────────────────
# positions.json holds your live trades.
# The example file (positions.example.json) is committed;
# your real positions.json is not.
positions.json

# ── Python ───────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.mypy_cache/
*.egg-info/
dist/
build/
.venv/
venv/
env/

# ── Logs ─────────────────────────────────────────
*.log
logs/

# ── OS ───────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
